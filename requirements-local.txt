# CPU-side dependencies for data, evaluation, analysis, and tests on macOS.
# The cluster requirements include CUDA wheels and should not be installed on a Mac.
datasets==5.0.0
transformers==5.12.1
torch>=2.7,<3
numpy==2.2.6
pandas==2.3.3
matplotlib==3.10.9
PyYAML==6.0.3
pytest>=8,<9
socksio==1.0.0
ruff==0.14.14
