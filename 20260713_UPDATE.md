# MLSys Course Project Plan

## 1. 核心研究问题

本项目研究：

> 在固定单 GPU 资源预算下，LoRA 相比全参数微调，在显存占用、训练吞吐量和任务效果之间提供怎样的权衡？

重点回答三个问题：

1. LoRA 能减少多少峰值 GPU 显存？
2. 节省的显存能否支持更大的 micro-batch，从而提高 tokens/s？
3. LoRA rank 和 sequence length 如何影响 memory–throughput–quality trade-off？

主要模型和任务：

* Model：Qwen3.5-0.8B，若兼容性存在问题则使用 Qwen3-0.6B
* Dataset：GSM8K
* Methods：Full Fine-tuning 与 LoRA
* Platform：单张 A40 48 GB GPU

核心指标：

* Peak GPU memory
* Non-padding tokens/s
* Training time
* GSM8K exact match
* Trainable parameter count
* Checkpoint size

---

## 2. 实现计划

建立统一训练框架，保证 Full FT 和 LoRA 使用相同的：

* 模型和数据划分
* Prompt 与 tokenization
* Max sequence length
* Effective batch size
* Precision
* Training token budget
* Evaluation pipeline

建议目录：

```text
src/
  train.py
  methods/
    full_ft.py
    lora.py
  data/
    gsm8k.py
  evaluation/
    exact_match.py
  metrics/
    memory.py
    throughput.py
  analysis/
    aggregate.py
    plot_results.py

configs/
scripts/
results/
```

每次实验输出统一 JSON，包括：

```json
{
  "method": "lora",
  "rank": 16,
  "max_length": 512,
  "micro_batch_size": 4,
  "peak_memory_gb": 0.0,
  "tokens_per_second": 0.0,
  "training_time_seconds": 0.0,
  "exact_match": 0.0,
  "trainable_parameters": 0,
  "checkpoint_size_mb": 0.0
}
```

---

## 3. 实验步骤

### Step 1：正确性验证

分别运行 Full FT 和 LoRA 的短训练：

* 20–50 training steps
* Micro-batch size = 1
* Max length = 512

检查：

* Loss 是否下降
* LoRA 是否只更新 adapter 参数
* Full FT 是否更新全部参数
* Checkpoint 是否可以重新加载
* 显存和时间测量是否正常
* Evaluation 是否可以正确计算 exact match

### Step 2：主实验

比较：

* Full FT
* LoRA，rank = 16

保持相同的 effective batch size 和 training token budget，记录：

* Peak memory
* Tokens/s
* Training time
* Exact match
* Checkpoint size

### Step 3：最大可运行 batch 实验

依次测试：

\[
B_{\text{micro}}\in{1,2,4,8,16}
\]

直到 OOM。

比较 Full FT 和 LoRA 的：

* Maximum feasible micro-batch
* Maximum tokens/s
* Peak memory

这是项目最重要的 systems 实验，用于判断 LoRA 的显存节省能否真正转化为吞吐量提升。

### Step 4：LoRA rank 扫描

测试：

\[
r\in{4,8,16,32}
\]

比较：

* Trainable parameters
* Peak memory
* Tokens/s
* Exact match
* Checkpoint size

目标是找出 Pareto-efficient rank，而不是只寻找最高准确率。

### Step 5：Sequence length 扫描

测试：

\[
L_{\max}\in{256,512,1024}
\]

比较 Full FT 和 LoRA-16，分析 sequence length 增大后 activation memory 是否削弱 LoRA 的相对显存优势。

### Step 6：最终结果验证

选择三个配置：

* Full FT
* LoRA rank 8
* LoRA rank 16

使用 3 个随机种子运行，报告：

\[
\text{mean}\pm\text{standard deviation}
\]

---

## 4. 主要结果展示

主图建议使用：

* x-axis：Peak GPU memory
* y-axis：Non-padding tokens/s
* 每个点标注 GSM8K exact match
* 不同点表示 Full FT 和不同 LoRA rank

核心结论应回答：

> LoRA 节省了多少显存，以及这些显存节省是否允许更大的 batch size 和更高的训练吞吐量，同时保持接近 Full FT 的任务效果。

---

## 5. 两人分工

### Daoyuan：训练系统与性能测量

负责：

* 统一训练入口
* Full FT 与 LoRA 实现
* Optimizer、mixed precision 和 gradient accumulation
* GPU memory instrumentation
* Step time 和 tokens/s 测量
* OOM 检测
* Checkpoint 大小统计
* Slurm 脚本和 HPC 运行

主要交付：

```text
train.py
methods/
metrics/
scripts/
```

### Zhaokai：数据、评估与结果分析

负责：

* GSM8K preprocessing
* Prompt template 与 tokenization
* Evaluation generation
* Answer extraction 与 exact match
* 实验配置生成
* Rank 和 sequence-length sweep
* 结果聚合
* Mean/std 计算
* Plot 与 Pareto analysis

主要交付：

```text
data/
evaluation/
analysis/
configs/
```

### 共同负责

两人共同确定并检查：

* Primary systems question
* 实验公平性和控制变量
* 主实验配置
* 最终图表
* 结果解释
* Introduction、Discussion 和 Conclusion
* 最终答辩

所有核心代码通过 Pull Request 交叉 review，避免一人只写代码、另一人只写报告。

---

## 6. 推荐执行顺序

1. 固定模型、数据、指标和实验协议
2. 分别实现训练框架与评估框架
3. 完成 Full FT 和 LoRA smoke test
4. 跑通一组完整主实验
5. 扫描最大 micro-batch
6. 扫描 LoRA rank
7. 扫描 sequence length
8. 对代表配置运行 3 个随机种子
9. 汇总结果、绘图并撰写报告
